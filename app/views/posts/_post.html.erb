<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

  <p>
    <strong>Image:</strong>
    <%= image_tag post.image.variant(resize_to_limit: [500, 500]) if post.image.attached? %>
  </p>
</div>
